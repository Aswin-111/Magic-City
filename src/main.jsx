import React from 'react'
import ReactDOM from 'react-dom/client'
import MagicCity from './MagicCity'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MagicCity/>
  </React.StrictMode>,
)
